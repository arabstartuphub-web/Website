{
  "name": "ase-fetch-articles",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone RSS fetch script — runs in GitHub Actions, writes to Neon DB directly.",
  "scripts": {
    "fetch": "node fetch-articles.mjs"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0",
    "rss-parser": "^3.13.0"
  }
}
